---
deployment:
  tasks:
    - /bin/mkdir -p ~/comrades-backend
    - /bin/cp -R backend/. ~/comrades-backend/
    - /bin/cp -R frontend/dist/. ~/public_html/
    - /bin/cp .htaccess ~/public_html/
    - /bin/cp backend/.env.production ~/comrades-backend/.env
